const {
    addResolvers,
    removeResolvers,
    getProxyResolvers,
} = require("./resolvers.js");

const {
    clientResolvers,
} = require("./component.js");

module.exports = {
    addResolvers,
    removeResolvers,
    getProxyResolvers,
    clientResolvers,
};
